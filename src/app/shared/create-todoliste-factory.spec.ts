import { CreateTodolisteFactory } from './create-todoliste-factory';

describe('CreateTodolisteFactory', () => {
  it('should create an instance', () => {
    expect(new CreateTodolisteFactory()).toBeTruthy();
  });
});
